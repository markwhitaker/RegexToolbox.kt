# Configure Gradle daemon to use Java 17 via toolchain
# This ensures compatibility with Kotlin Gradle Plugin on systems with Java 25+
# Gradle will auto-download Java 17 if not available (via foojay-resolver in settings.gradle.kts)
toolchainVersion=17
